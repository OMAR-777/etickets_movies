@model string;

@if (User.Identity.IsAuthenticated && User.IsInRole(UserRoles.Admin))
{
    <div class="text-white floatingElement">
        <a asp-controller="@Model" asp-action="Create" class="btn btn-success">
            <i class="bi bi-plus-circle"></i> Add new
        </a>
    </div>
}